/** DI-токен для инжекции массива обработчиков событий блокчейна. */
export const EVENT_HANDLERS = Symbol('EVENT_HANDLERS');

/** DI-токен для инжекции массива обработчиков экстринсиков блокчейна. */
export const EXTRINSIC_HANDLERS = Symbol('EXTRINSIC_HANDLERS');
